{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python -c \"from alembic.config import main; main(argv=['upgrade','head'])\" && python -m uvicorn ai_candidate_screening_outreach.app:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
